const TestComponents = (props) => {
  // const course = "React";
  return (
    <div>
      <p>Welcome to {props.course} course</p>
      <p>My name is {props.name}</p>
    </div>
  );
};

export default TestComponents;
